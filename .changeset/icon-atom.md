---
"@behnamsobhkhiz/sonnat-react": minor
---

Add the `Icon` atom: ~800 tree-shakeable SVG glyph components migrated from the legacy SONNAT icon font (no icon font ships), plus a token-aware `<Icon>` wrapper (render by `name` via registry or by `glyph` for tree-shaking; size on the type scale; color via `--content-*`; accessible `label`). Includes Storybook stories (searchable gallery) and `COMPONENT.md`.
